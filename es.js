const first = "A";
const last = "B";
// const name = first + " " + last;
const name = `${first}  ${last}`;

// console.log(name);

const songName = 'amar sonar bangla';

// console.log(songName);

const numbers = [20, 22, 25, 36];

const student = { name: 'meri', age: 32 };

const math = ` the sum of ${numbers[0]} and ${student.age} is ${numbers[0] + student.age}`

console.log(math);